# Destiny Analytical System - Python Dependencies

# Core AI/ML
anthropic>=0.7.0          # Claude API for supervision
numpy>=1.24.0             # Numerical operations

# Database Clients
psycopg2-binary>=2.9.9    # PostgreSQL
redis>=5.0.0              # Redis cache

# Environment & Configuration
python-dotenv>=1.0.0      # Environment variables

# HTTP & Networking (for custom clients)
urllib3>=2.0.0            # HTTP client

# Data Processing
dataclasses-json>=0.6.0   # JSON serialization

# Testing
pytest>=7.4.0             # Testing framework
pytest-asyncio>=0.21.0    # Async testing

# Optional: If you want to install packages for direct integration
# elasticsearch>=8.0.0    # Elasticsearch client
# py2neo>=2021.2.3        # Neo4j client (alternative)

# Development
black>=23.0.0             # Code formatting
flake8>=6.0.0            # Linting
mypy>=1.0.0              # Type checking

# Note: Some packages intentionally NOT included to avoid conflicts
# - We use urllib for HTTP to avoid aiohttp installation issues
# - Qdrant uses standard HTTP endpoints (no special client needed)
# - Neo4j uses HTTP API (no driver needed for basic ops)
